{
  "name": "sharma-mobile-repair",
  "version": "1.0.0",
  "description": "Mobile repair service website with Firestore integration",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "vitest": "^1.0.4",
    "@vitest/ui": "^1.0.4"
  }
}
